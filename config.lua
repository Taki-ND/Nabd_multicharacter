Config = {}

-----------------------------------------------------------------------------
-- إعدادات اسم الديسكورد
-----------------------------------------------------------------------------
Config.DiscordName = {
    -- إذا كانت true، بيحاول يجيب اسم اللاعب من الديسكورد (يحتاج بوت توكن)
    Enabled = true,

    -- توكن بوت الديسكورد الخاص فيك (Discord Developer Portal -> Applications -> Bot -> Token)
    -- لازم يكون فيه صلاحية قراءة بيانات المستخدمين، ولا يحتاج البوت يكون بنفس السيرفر
    -- خلها فاضية '' إذا ما تبي تستخدم هالخاصية
    BotToken = '',

    -- إذا فشل الاتصال بالديسكورد أو ما كان فيه توكن، يرجع لاسم اللاعب داخل اللعبة (Rockstar/Steam)
    FallbackToGameName = true,
}

-----------------------------------------------------------------------------
-- بيانات الشخصية الافتراضية (تنحط تلقائي على أي شخصية جديدة)
-----------------------------------------------------------------------------
Config.DefaultCharInfo = {
    lastname    = 'Nabd',      -- اللقب الافتراضي، غيّره حسب رغبتك
    birthdate   = '2000-01-01',    -- تاريخ ميلاد افتراضي (yyyy-mm-dd)
    gender      = 0,               -- 0 = ذكر / 1 = أنثى
    nationality = 'سعودي',
}

-----------------------------------------------------------------------------
-- الإحداثيات
-----------------------------------------------------------------------------
-- مكان يتحط فيه اللاعب (مجمد وظاهر أمام كاميرا الشخصية) أثناء ظهور واجهة "ابدأ"
-- w = الاتجاه (heading) اللي تكون الشخصية واقفة فيه مقابل الكاميرا
Config.HiddenCoords = vector4(-269.4, -955.9, 300.0, 160.0)

-- مكان ظهور اللاعب بعد الضغط على "ابدأ" (x, y, z, heading)
Config.DefaultSpawn = vector4(-1035.71, -2731.87, 20.16, 327.62)

-----------------------------------------------------------------------------
-- إحصائيات (قتلات / وفيات) تظهر بواجهة الشخصية - اختياري
-----------------------------------------------------------------------------
-- إذا عندك جدول إحصائيات جاهز (مثلاً من qb-pvp) فعّل هذا واربطه، غير كذا
-- تظل القيمة 0/0 دايماً (مناسب لشخصية جديدة)
Config.Stats = {
    Enabled          = false,
    Table            = 'player_stats',   -- اسم الجدول
    CitizenIdColumn  = 'citizenid',      -- عمود ربط الشخصية
    KillsColumn      = 'kills',          -- عمود القتلات
    DeathsColumn     = 'deaths',         -- عمود الوفيات
}

-----------------------------------------------------------------------------
-- عناصر ابتدائية (اختياري) تنعطى للاعب عند إنشاء شخصيته لأول مرة فقط
-----------------------------------------------------------------------------
Config.EnableStarterItems = false
Config.StarterItems = {
    -- { name = 'water_bottle', amount = 2 },
    -- { name = 'sandwich', amount = 2 },
    -- { name = 'phone', amount = 1 },
}

-----------------------------------------------------------------------------
-- إشعار ديسكورد (ويبهوك) عند إنشاء شخصية جديدة - اختياري
-----------------------------------------------------------------------------
Config.Webhook = '' -- مثال: 'https://discord.com/api/webhooks/xxx/xxx'

-----------------------------------------------------------------------------
-- رسائل
-----------------------------------------------------------------------------
Config.NoLicenseKickMessage = 'تعذر التحقق من هويتك (License)، حاول تعيد الاتصال بالسيرفر.'
Config.LoginErrorKickMessage = 'حدث خطأ أثناء تحميل بياناتك، حاول تعيد الاتصال بالسيرفر.'
